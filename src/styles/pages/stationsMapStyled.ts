import { makeStyles } from "@material-ui/core"

export const stationsMapStyled = makeStyles(() => ({
  pageMap: {
    width: "100vw",
    height: "100vh",
    position: "relative",
    display: "flex",
  },
}))
